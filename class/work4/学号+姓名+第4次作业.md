# 第四次作业

编程

# 1.计算一下你活了多少天？

输入你出生日期（年月日），根据C语言自带的库函数获取当前日期，两者相减即为你活了多少天。

```c
/*struct tm	存储年、月、日、时、分、秒的结构体
/*time(&)	获取当前时间戳（秒数）
/*localtime()	把时间戳转成 struct tm 格式
/*通常用time_t存放秒数
/*mktime()	把 struct tm 转成时间戳
/*difftime()	计算两个时间戳的差值（秒）
    \\struct tm {
    \\int tm_sec;   // 秒
    \\int tm_min;   // 分
    \\int tm_hour;  // 时
    \\int tm_mday;  // 日 ← 注意是 mday，不是 day
    \\int tm_mon;   // 月 ← 注意是 mon，不是 month
    \\int tm_year;  // 年
    \\int tm_wday;  // 星期几
    \\int tm_yday;  // 年内第几天
    \\int tm_isdst; // 夏令时
};
*/
#include <stdio.h>
#include <time.h>
int main ()
{
    struct tm birth={0};
    int year,month,day ;
    printf("enter your birthday");
    scanf ("%d %d %d",&year,&month,&day);
    birth.tm_year=year-1900;
    birth.tm_mon=month-1;
    birth.tm_mday=day;
    time_t now=time(NULL);
    time_t tmd =mktime(&birth);
    time_t s=now-tmd ;
    int days = (int)(s / (60 * 60 * 24));
    if(days<0) return 0;
    printf ("%d",days);
}
```

## 2.计算你女（男）朋友下一个生日还有多少天？

输入你女（男）朋友（月，日），根据C语言自带的库函数获取当前日期，计算下一个生日还有多少天。

```c
#include <stdio.h>
#include <time.h>
int main ()
{
    struct tm birth={0};
    int month,day;
    printf("enter your friend birthday");
    scanf("%d %d",&month,&day);
    birth.tm_mon=month-1;
    birth.tm_mday=day;
    time_t now=time(NULL);
    struct tm *years =localtime (&now);
    birth.tm_year=years->tm_year;
    time_t birth_s=mktime(&birth);
    time_t s=now-birth_s;
    int days=(int)(s/(60*60*24));
    if(days <0)
    {
        days=days+365;
    }
    printf("%d",days);
    return 0;
}
```

## 3.这是一道蓝桥杯竞赛题。1949年10月1日是周六，计算一下从1949~2025年，多少个国庆日和周日重逢。

```c
#include <stdio.h>
#include <time.h>
int main()
{
    struct tm dayfrist={0};
    int count=0;
    for (int year=49;year<125;year++)
    {
        dayfrist.tm_year=year;
        dayfrist.tm_mon=9;
        dayfrist.tm_mday=1;
        mktime(&dayfrist);//用来更新用的
        if(dayfrist.tm_wday==0)
        {
            count++;
        }
    }
     printf("All in %d",count);
    return 0;
}
```

## 4.求倒数

输入一个正整（N>1），求它的倒数。要求，

- 有限小数，如1/2，输出[5],-1。 其中-1表示不循环。
- 无限循环小数，如1/7 输出[1,4,2,8,5,7] 0 其中0表示从index = 0开始循环。

  如1/6 输出[1,6] 1  其中1表示从index=1开始循环。

  ```c

  ```
